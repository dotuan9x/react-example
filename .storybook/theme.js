import { create } from '@storybook/theming/create';
import logo from './assets/storybook.svg';

export default create({
    base: 'light',
    brandTitle: 'React Storybook example',
    brandUrl: 'https://tuando.net',
    brandImage: logo
});
